html
	head
		style
			include style.css
		block scripts
			link(rel="stylesheet", href="https://maxcdn.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css")
			link(rel="stylesheet", href="https://maxcdn.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap-theme.min.css")
			script(src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.3/umd/popper.min.js")
			script(src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js")
			script(src="https://maxcdn.bootstrapcdn.com/bootstrap/4.1.3/js/bootstrap.min.js")
			

	body
		
		section(id="conteneur")
			div(id='element2')
				h1(id="MainTitle") Monkey 
				h1(id="MainTitle") Manager
				h1(id="MainTitle") 3000
			div(id='element2')
				-	test  = new Date()
				-	var months = ["Janvier", "Fevrier", "Mars", "Avril", "Mai", "Juin", "Juillet", "Aout", "Septembre", "Octobre", "Novembre", "Decembre"];
				- 	var days = ["Dimanche", "Lundi", "Mardi", "Mercredi", "Jeudi", "Vendredi", "Samedi" ]
				-	var Minute = test.getMinutes() 
				-	if( test.getMinutes() < 10){ 
				-	Minute = '0' +test.getMinutes()
				-	}
				

				
				
				h1(id="MainTitle")=test.getHours() + " : " + Minute
				h3=days[test.getDay()] + " " +   test.getDate()   + " " + months[(test.getMonth())] 
			div(id='element2')
				na
			div(id='element2')
				p na
			div(id='element1')
				block ListeA
			div(id="sousConteneur")
				div(id="element4")
					block creationContent
				div(id="element4")
					block detail
			div(id='element1')
				block ListeB
	

	footer
		
		p This is footer !